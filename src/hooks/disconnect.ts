import { ConnectionSocket } from '@/views/Room/signalling';

export const useSendDisconnect = () => {
  // const sendDisconnect = (ws: ConnectionSocket, roomID: string) => {
  //   ws.send('disconnect', roomID);
  // };

  // return { sendDisconnect };
};


